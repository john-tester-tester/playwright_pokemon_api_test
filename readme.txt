open in something like visual studio code, open terminal and install playwright

npm install @playwright/test

then run the test..


npm run tests:api


(Look in package.json', script section shows what config is run.   Look in api.config.ts, to see more of the run config options)